Review only the eigenstate-selection subsystem in the uploaded files.

Reply in Simplified Chinese.

Focus on:

1. whether each selection method matches the standard,
2. whether the objective optimized during selection is actually the final
   objective reported,
3. whether block-wise selection and reconstructed full-spectrum selection are
   consistent,
4. whether there are correctness risks that would affect the physical validity
   of `H_eff`,
5. whether existing tests cover the riskiest logic.

Be adversarial and skeptical.

Output findings only, ordered by severity.

For each finding, give:

- file and function,
- why it is a real risk,
- the exact condition under which it appears,
- the smallest code or test change that would verify or fix it.

Do not give generic style advice.
If there are no concrete findings, say exactly: `No concrete findings.`
